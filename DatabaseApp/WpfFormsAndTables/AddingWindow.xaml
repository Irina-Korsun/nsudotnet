<Window x:Class="WpfFormsAndTables.AddingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AddingWindow" Height="300" Width="528">
    <Grid>
        <ListBox x:Name="FieldsListBox" Margin="0,0,0,158">
            <ListBox.ItemTemplate>
                <DataTemplate x:Name="Data">
                    <StackPanel x:Name="Stack" Orientation="Horizontal">
                        <Label Content="{Binding Name}" VerticalAlignment="Center"/>
                        <TextBox x:Name="Text" Text="Type here" Width="80" Margin="5,0,0,0"/>
                        <Label Content="*"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" 
                           Height="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Path=ActualHeight}"
                           Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Path=ActualWidth}"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button Content="Add" HorizontalAlignment="Left" Height="25" Margin="430,124,0,0" VerticalAlignment="Top" Width="80" Click="Add_Click"/>
    </Grid>
</Window>
